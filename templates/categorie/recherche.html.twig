{% extends 'base.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}
<td><button><a href="{{ path('categorie') }}">Retour</a></button></td>

    <h1>Categories</h1>

    <table class="table">
        <tbody>
    {% for categorie in categories %}
            <tr>
                <th>Id</th>
                {# <td>{{ categorie.id }}</td> #}
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ categorie.Titre }}</td>
            </tr>
            <tr>
                <th>Resume</th>
                <td>{{ categorie.Resume }}</td>
            </tr>
    {% else %}             
        </tbody>
    </table>
{% endblock %}
