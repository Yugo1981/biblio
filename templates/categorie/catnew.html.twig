{% extends 'base.html.twig' %}

{% block body %}

{{ form_start(formCategorie) }}
  {{ form_widget(formCategorie.Titre) }}<br>
  {{ form_widget(formCategorie.Resume) }} <br>

  <button type="submit" class="btn btn-success">envoyer</button>

    {{ form_end(formCategorie) }}
{% endblock %}

