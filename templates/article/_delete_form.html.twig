<form method="post" action="{{ path('delete_article', {'id': article.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.id) }}"><br>
    <button class="btn">Supprimer</button>
</form>
